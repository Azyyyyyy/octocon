version: '3.8'

services:
  interfold-api:
    image: interfold-api:latest
    build:
      context: ..
      dockerfile: csharp/Interfold.Api/Dockerfile
    ports:
      - "8081:8080"
    environment:
      # Add any required environment variables here
      ASPNETCORE_ENVIRONMENT: Development
    restart: unless-stopped
